Mget is a lightweight multithreaded metalink/file/website downloader
written in C (C99 / Posix 2001 compliant).

Use './configure && make' to compile.
(You might need a 'autoreconf -i' the very first time.)

Have a look into 'README.rst' about more details.
